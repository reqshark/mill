#!/usr/bin/env node
var exec = require('child_process').exec
//require('..')(process.argv)
console.log('util link test\n\n')
exec('which nanocat',function (err, stdout, stderr){
  if(stderr) return console.log(stderr)
  if(stdout) return console.log(stderr)
})
